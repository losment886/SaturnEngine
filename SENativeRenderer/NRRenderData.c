#include "NRDefine.h"

// ============================================================
// NRRenderData.c
// 渲染数据定义：立方体顶点/索引
// ============================================================

// ============================================================
// 立方体顶点数据（相对物体坐标，中心在原点，边长 1）
// 8 个顶点
// ============================================================

const f32 g_CubeVertices[CUBE_VERTEX_COUNT * 3] = {
    // x,    y,    z
    -0.5f, -0.5f, -0.5f, // 0: 左下后
     0.5f, -0.5f, -0.5f, // 1: 右下后
     0.5f,  0.5f, -0.5f, // 2: 右上后
    -0.5f,  0.5f, -0.5f, // 3: 左上后
    -0.5f, -0.5f,  0.5f, // 4: 左下前
     0.5f, -0.5f,  0.5f, // 5: 右下前
     0.5f,  0.5f,  0.5f, // 6: 右上前
    -0.5f,  0.5f,  0.5f, // 7: 左上前
};

// 36 个索引（6 个面，每个面 2 个三角形，每个三角形 3 个顶点）
const u32 g_CubeIndices[CUBE_INDEX_COUNT] = {
    // 前面 (z=0.5)
    4, 5, 6,
    4, 6, 7,
    // 后面 (z=-0.5)
    1, 0, 3,
    1, 3, 2,
    // 上面 (y=0.5)
    3, 7, 6,
    3, 6, 2,
    // 下面 (y=-0.5)
    4, 0, 1,
    4, 1, 5,
    // 右面 (x=0.5)
    5, 1, 2,
    5, 2, 6,
    // 左面 (x=-0.5)
    4, 7, 3,
    4, 3, 0,
};
